// AI Use Cases — Phase 2
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Use Cases — KI-Anwendungsfälle für Unternehmen",
};

export default function AIUseCasesPage() {
  return (
    <main className="flex-1 p-8">
      <p className="text-foreground">AI Use Cases — Phase 2</p>
    </main>
  );
}
