// Kontaktformular API — Wird in Schritt 3 implementiert
import { NextRequest } from "next/server";

export async function POST(request: NextRequest) {
  void request;
  return Response.json({ success: true, data: null });
}
